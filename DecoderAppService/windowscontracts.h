#pragma once

/**
 * Solves bug:
 *
 * When building WRL projects, your code may fail to compile due to a missing 
 * file: WindowsContracts.h. This will be addressed in the next update. To 
 * work around this, create an empty header file: WindowsContracts.H and 
 * include it in your include Search path.
 */
